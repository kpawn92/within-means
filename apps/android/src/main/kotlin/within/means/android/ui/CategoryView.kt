package within.means.android.ui

/** Lightweight view of a category for rendering rows: name + glyph id + colour hex. */
data class CategoryView(val name: String, val icon: String, val color: String)
